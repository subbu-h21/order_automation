CRM_USERNAME=
CRM_PASSWORD=
CRM_USERNAME_SHIVAJI_CHOWK=
CRM_PASSWORD_SHIVAJI_CHOWK=
CHROME_PROFILE_DIR=C:\AutomationProfile_LiveConnect

# Random secret that signs the dashboard login session cookie. Generate once with:
#   python -c "import secrets; print(secrets.token_hex(32))"
SESSION_SECRET_KEY=

# Staff accounts allowed to log into the dashboard. Format: user1:salt$hash,user2:salt$hash
# Generate each salt$hash by running (with the venv active, from order_automation_v2):
#   python hash_password.py
DASHBOARD_USERS=
